# Steps to Recovery - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# Supabase Configuration (REQUIRED)
# =============================================================================
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# OpenClaw AI Gateway (OPTIONAL - self-hosted VPS)
# =============================================================================
# Run your own AI gateway on a VPS (e.g. Hostinger) using LiteLLM or similar.
# When set, OpenClaw takes priority over all other AI providers.
#
# Setup on your VPS:
#   1. Install LiteLLM:  pip install litellm[proxy]
#   2. Start proxy:      litellm --model gpt-4o-mini --port 4000
#   3. Set a token:      Use any secret string as OPENCLAW_TOKEN
#
# ⚠️  SECURITY: NEVER ship a production build with EXPO_PUBLIC_OPENCLAW_TOKEN set.
#     EXPO_PUBLIC_* vars are embedded in the JS bundle and extractable from APKs.
#     For production use: configure via the in-app Settings screen instead —
#     values are stored securely in device Keychain/Keystore.
#
# For development / admin use only:
EXPO_PUBLIC_OPENCLAW_TOKEN=
EXPO_PUBLIC_OPENCLAW_URL=

# Enable proxy mode — routes ALL users through your Supabase edge function
# which forwards securely to OPENCLAW_URL on your VPS (token never in APK).
# Set to "true" for production builds so users need zero setup.
# Requires: supabase secrets set OPENCLAW_URL=... OPENCLAW_TOKEN=...
EXPO_PUBLIC_AI_PROXY_ENABLED=false

# =============================================================================
# Sentry Error Tracking (OPTIONAL - for production monitoring)
# =============================================================================
# Sign up at https://sentry.io and create a React Native project
# Leave blank to disable error tracking

EXPO_PUBLIC_SENTRY_DSN=

# =============================================================================
# Environment Mode (OPTIONAL)
# =============================================================================
# Options: development | staging | production
# Default: development

EXPO_PUBLIC_ENV=development

# =============================================================================
# Setup Instructions
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Create a Supabase project at https://supabase.com
#
# 3. Run the database migrations:
#    - Go to SQL Editor in Supabase dashboard
#    - Run supabase-schema.sql (base schema)
#    - Run supabase-migration-*.sql files (features)
#
# 4. Copy your Supabase URL and anon key from:
#    Settings > API > Project URL and anon/public key
#
# 5. (Optional) Set up Sentry for error tracking:
#    - Create account at https://sentry.io
#    - Create React Native project
#    - Copy DSN to EXPO_PUBLIC_SENTRY_DSN
#
# 6. Start the app:
#    npm run mobile
#
# =============================================================================
# Security Notes
# =============================================================================
#
# ✅ SAFE TO COMMIT: .env.example (no secrets)
# ❌ NEVER COMMIT: .env (contains real keys)
#
# The Supabase anon key is safe to expose in the client app because:
# - Row-Level Security (RLS) policies protect all data
# - Client-side encryption used for sensitive data
# - Anon key has very limited permissions
#
# The encryption master key is NEVER stored in .env - it's derived from
# the user's session token and stored in device secure storage (Keychain/Keystore)
#
